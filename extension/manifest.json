{
  "manifest_version": 3,
  "name": "Lennar CVRMLS Matrix Filler (POC)",
  "version": "0.2.0",
  "description": "Side panel that detects the active CVRMLS Matrix tab and fills it from a pasted JSON payload. Persists across Matrix's per-tab page reload. Lennar/CVRMLS scoped POC.",
  "permissions": ["activeTab", "sidePanel", "storage"],
  "action": {},
  "background": {
    "service_worker": "background.js"
  },
  "side_panel": {
    "default_path": "sidepanel.html"
  },
  "content_scripts": [
    {
      "matches": ["https://cvrmls.mlsmatrix.com/Matrix/Input/InputForm.aspx*"],
      "js": ["content.js"],
      "run_at": "document_idle"
    }
  ]
}
