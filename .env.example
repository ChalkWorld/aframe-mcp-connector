# Aframe Open API key
# Get from Aframe → My Profile → API Keys → Developer Zone
AFRAME_API_KEY=your_api_key_here

# Port for local dev. Railway assigns this dynamically via $PORT in production —
# leave unset there.
PORT=3000
